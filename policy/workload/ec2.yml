policies:
  - name: ec2-instances
    description: |
      Checks for ec2 instances
    resource: ec2
    region: ca-central-1
    # action:
    #   # send notification to security hub
    #   - type: post-finding
    #     description: |
    #         EC2 instances are not allowed.
    #     severity_normalized: 6
    #     confidence: 100
    #     compliance_status: FAILED